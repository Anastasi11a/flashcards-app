import { headerStyle } from "@/utils/navigationStyles";

export const HEADER_HEIGHT = (headerStyle.height as number) ?? 120;

export const FOLDER_ITEM_HEIGHT = 80;
export const FOLDER_BAR_PADDING_BOTTOM = 12;
export const FOLDER_BAR_HEIGHT = FOLDER_ITEM_HEIGHT + FOLDER_BAR_PADDING_BOTTOM;

export const PADDING_TOP = 110;